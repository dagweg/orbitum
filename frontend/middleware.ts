import { NextRequest, NextResponse } from "next/server";
// import otpPageMiddleware from "./middlewares/otpPageMiddleware";

export default function middleware(req: NextRequest, res: NextResponse) {
  // otpPageMiddleware(req, res);
}

// export const protectedRoutes = ["/auth/otp"];

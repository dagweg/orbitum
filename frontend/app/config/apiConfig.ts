/**
 * Change the below constants to reflect the acutal deployment urls
 */
export const API_ORIGIN: string = process.env.API_ORIGIN || "http://localhost:5000";
export const CLIENT_ORIGIN: string =
  process.env.NEXT_PUBLIC_URL || "http://localhost:3000";
